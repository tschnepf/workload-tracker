/**
 * @deprecated Use src/theme/contract.ts for all theme token definitions.
 * Kept as a compatibility shim for existing imports.
 */

export {
  THEME_TOKEN_GROUPS,
  THEME_COLOR_SCHEMES,
  cssVar,
  type ThemeTokenGroup,
  type ThemeTokenName,
  type ThemeColorScheme,
} from './contract';
